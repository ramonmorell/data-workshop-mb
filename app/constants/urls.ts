const BASE = "http://192.168.88.207"; // Server data-workshop-be
const PORT = "8762"; // Zuul server data-workshop-be

const URLS = {
  PROJECT: `${BASE}:${PORT}/api/project`,
  PROJECTS: `${BASE}:${PORT}/api/projects`,
};

export default URLS;
